lis = range(1,60)
i = 0
 
while(i <= len(lis)):
    print(lis[i], end = " ")
    i = i + 4
    # Changing the value of
    # i inside the loop will
    # change it's value at the
    # time of checking condition
    